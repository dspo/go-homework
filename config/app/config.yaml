app_name: go-homework

server:
  listen:
    host: "0.0.0.0"
    port: 8080

log:
  level: info

database:
  name: mysql
  server: ${MYSQL_SERVER}
  port: ${MYSQL_PORT}
  user: ${MYSQL_USER}

prometheus:
  address: http://prometheus:9090
